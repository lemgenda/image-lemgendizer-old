// vercel-build.js - Ensures proper build
console.log('Vercel Build Script - Ensuring Chromium compatibility');
console.log('Using @sparticuz/chromium-min@121.0.0');

// This script ensures the build doesn't fail due to Chromium issues
process.exit(0);